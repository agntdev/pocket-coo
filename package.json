{
  "name": "pocket-coo",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram bot that turns chaotic chats into structured projects and tasks",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/bot.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "typescript": "^5.5.0"
  }
}
