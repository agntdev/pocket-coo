# fix-7575a461e4c7d99e — Fix: Data export flow as triggered by `/export` is not described (only mentioned in acceptance, but no screen/transition).

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 PCOO

missing: Data export flow as triggered by `/export` is not described (only mentioned in acceptance, but no screen/transition).

Severity: medium.