# fix-d383873e057132c3 — Fix: Reminder Sweep timing contradicts PRIOR_DOC: CURRENT_DOC sets condition `deadline <= now + 24h` and Acceptance Note 3 states 'Overdue follow-ups must trigger DMs 24h before deadline', whereas PRIOR_DOC explicitly defines the sweep for `deadline <= now` and `notified_at IS NULL` (alert at due mo

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 PCOO

contradiction: Reminder Sweep timing contradicts PRIOR_DOC: CURRENT_DOC sets condition `deadline <= now + 24h` and Acceptance Note 3 states 'Overdue follow-ups must trigger DMs 24h before deadline', whereas PRIOR_DOC explicitly defines the sweep for `deadline <= now` and `notified_at IS NULL` (alert at due moment, not 24h ahead).

Severity: high.